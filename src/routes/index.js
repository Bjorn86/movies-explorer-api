// IMPORT PACKAGES
const rootRouter = require('express').Router();

// IMPORT ROUTES

// IMPORT MIDDLEWARES
const auth = require('../middlewares/auth');

// ROUTES METHODS

// EXPORT ROUTES
module.exports = rootRouter;
